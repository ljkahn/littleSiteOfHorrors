    <section class="py-5">
        <div class="container px-4 px-lg-5 my-5">
            <div class=" text row gx-4 gx-lg-5 align-items-center">
                <div class="col-md-6"><img src={{poster_url}}class="img-fluid img-thumbnail" alt="..."></div>
                <div class="col-md-6">
                    <h1 class="display-5 fw-bolder">{{title}}</h1>
                    <h2 class="display-5 fw-bolder">{{director}}</h2>
                    <h3 class="display-5 fw-bolder">{{release_year}}</h3>
                    <div class="fs-5 mb-5">
                    </div>
                    <p class="lead">{{description}}</p>
                    <br />
                    <div class="d-flex flex-column">
                       <button
                        class="d-flex btn btn-log1 text-white w-100 py-2"
                        type="button" id="add-to-fav" >
                        Add to Favorites
                        </button>
                        <br />
                        <button
                        class="d-flex btn btn-log1 text-white w-100 py-2"
                        type="button">
                        post a review
                        </button>
                         <br />
                    </div>
                    <h1 class="display-5 fw-bolder">Spooky Scale: <span class="badge bg-secondary">{{rating}}</span></h1>
                </div>
            </div>
        </div>
    </section>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="/js/oneSearchResult.js"></script>